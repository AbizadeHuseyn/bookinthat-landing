{
  "name": "bookinthat-landing",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "resend": "^4.0.0"
  },
  "scripts": {
    "dev": "npx serve public -l 3000",
    "build": "echo 'Static site — no build step required'"
  }
}
